# MLOps Foundation: S3 buckets (raw, features, models) + IAM/IRSA for ML workloads
# Step 1 - to be implemented

variable "tags" {
  description = "Tags to apply."
  type        = map(string)
  default     = {}
}
